var greet = "Hello";
var name = "Bruce!";

// only change code below this line
var there="there,";
var str = greet + ' ' + there +' '+name;

// only change code above this line

module.exports = { str }